# Netlify configuration for R2C Vite Frontend
# This automates build settings and configures routing redirects for React Router SPAs.

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
